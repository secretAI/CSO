export enum TokenTypes {
  access = "JWT_SECRET",
  refresh = "JWT_REFRESH"
}

export enum ProductProperty {
  "title",
  "startingPrice",
  "type",
  "discount",
  "duration"
}

export enum ProductTypes {
  "service",
  "subscription"
}

